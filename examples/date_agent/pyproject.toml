[project]
name = "date-agent"
version = "0.1.0"
description = "Date Agent example for OpenSymbolicAI"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
