[project]
name = "unit-converter"
version = "0.1.0"
description = "Unit Converter example for OpenSymbolicAI"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core",
    "tqdm>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
